#name: Release Maven
#
#on:
#  workflow_dispatch:
#  push:
#    branches:
#      - master
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v1
#      - name: Set up JDK 11
#        uses: actions/setup-java@v2
#        with:
#          distribution: 'adopt'
#          java-version: '11'
#          server-id: maven
#          server-username: MAVEN_USERNAME
#          server-password: MAVEN_CENTRAL_TOKEN
#          gpg-private-key: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
#          gpg-passphrase: MAVEN_GPG_PASSPHRASE
#
#      - name: Publish to Apache Maven Central
#        run: |
#          mkdir -p ~/.m2
#          echo "<settings><servers><server><id>\${env.ID}</id><username>\${env.MAVEN_USERNAME}</username><password>\${env.MAVEN_CENTRAL_TOKEN}</password></server><server><id>gpg.passphrase</id><passphrase>\${env.MAVEN_GPG_PASSPHRASE}</passphrase></server></servers></settings>" > ~/.m2/settings.xml
#          mvn clean deploy -P release -DskipTests
#        env:
#          ID: maven
#          MAVEN_USERNAME: hocgin
#          MAVEN_CENTRAL_TOKEN: ${{ secrets.MAVEN_CENTRAL_TOKEN }}
#          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
